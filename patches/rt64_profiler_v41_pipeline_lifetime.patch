diff --git a/src/common/rt64_performance_profiler.cpp b/src/common/rt64_performance_profiler.cpp
index f0d7d7e..ca61f1c 100644
--- a/src/common/rt64_performance_profiler.cpp
+++ b/src/common/rt64_performance_profiler.cpp
@@ -4346,7 +4346,7 @@ namespace RT64 {
                 return false;
             }
 
-            output << "{\n  \"schema\": \"rt64-performance-summary-v40\",\n";
+            output << "{\n  \"schema\": \"rt64-performance-summary-v41\",\n";
             output << "  \"capture_mode\": \""
                 << (profiler.lowOverhead ? "low_overhead" : "detailed")
                 << "\",\n";
@@ -6365,6 +6365,28 @@ namespace RT64 {
         appendEvent(std::move(event));
     }
 
+    void PerformanceProfiler::recordCriticalInstant(
+        PerformanceCategory category,
+        std::string_view name,
+        uint64_t correlationId,
+        uint64_t argument0,
+        uint64_t argument1
+    ) {
+        if (!enabled()) {
+            return;
+        }
+
+        Event event;
+        copyEventName(event, name);
+        event.timestampNanoseconds = nowNanoseconds();
+        event.correlationId = correlationId;
+        event.argument0 = argument0;
+        event.argument1 = argument1;
+        event.category = category;
+        event.kind = EventKind::Instant;
+        appendEvent(std::move(event));
+    }
+
     void PerformanceProfiler::recordCounter(
         std::string_view name,
         double value,
diff --git a/src/common/rt64_performance_profiler.h b/src/common/rt64_performance_profiler.h
index 02c8de0..a99831e 100644
--- a/src/common/rt64_performance_profiler.h
+++ b/src/common/rt64_performance_profiler.h
@@ -67,6 +67,14 @@ namespace RT64 {
             uint64_t argument0 = 0,
             uint64_t argument1 = 0
         );
+        // Records lifecycle and synchronization boundaries even in the low-overhead flight-recorder mode.
+        static void recordCriticalInstant(
+            PerformanceCategory category,
+            std::string_view name,
+            uint64_t correlationId = 0,
+            uint64_t argument0 = 0,
+            uint64_t argument1 = 0
+        );
         static void recordCounter(
             std::string_view name,
             double value,
diff --git a/src/hle/rt64_present_queue.cpp b/src/hle/rt64_present_queue.cpp
index 9aeb45f..1fa7211 100644
--- a/src/hle/rt64_present_queue.cpp
+++ b/src/hle/rt64_present_queue.cpp
@@ -469,7 +469,9 @@ namespace RT64 {
                 RenderCommandListType::DIRECT,
                 ext.presentGraphicsWorker
             );
-            slot.viRenderer = std::make_unique<VIRenderer>();
+            slot.viRenderer = std::make_unique<VIRenderer>(
+                "Present Slot " + std::to_string(i) + " Video Interface"
+            );
             slot.acquiredSemaphore =
                 ext.device->createCommandSemaphore();
             if (performanceGPUInstrumentationEnabled) {
@@ -587,6 +589,13 @@ namespace RT64 {
         if (slot.captureComplete != nullptr) {
             slot.captureComplete();
         }
+        if (slot.renderHookSubmissionId != 0u) {
+            RenderHookDrawComplete *drawComplete =
+                GetRenderHookDrawComplete();
+            if (drawComplete != nullptr) {
+                drawComplete(slot.renderHookSubmissionId);
+            }
+        }
         {
             const std::scoped_lock<std::mutex> retirementLock(
                 presentationRetirementMutex
@@ -597,6 +606,7 @@ namespace RT64 {
             slot.presentId = 0;
             slot.workloadId = 0;
             slot.targetSetId = 0;
+            slot.renderHookSubmissionId = 0;
             slot.measureGPU = false;
             slot.submitted = false;
         }
@@ -792,7 +802,9 @@ namespace RT64 {
             performanceQueryPool = ext.device->createQueryPool(5);
         }
 
-        viRenderer = std::make_unique<VIRenderer>();
+        viRenderer = std::make_unique<VIRenderer>(
+            "Present Shared Video Interface"
+        );
 
         presentationRetirementThreadRunning = true;
         presentationRetirementThread = new std::thread(
@@ -1376,6 +1388,7 @@ namespace RT64 {
                     )
                     : std::chrono::nanoseconds(0);
             RenderHookCaptureComplete *captureComplete = nullptr;
+            uint64_t drawHookSubmissionId = 0;
             bool presentWorkSubmitted = false;
             if (presentFrame && consumesInterpolationSequence) {
                 ensurePresentationSlots();
@@ -1536,7 +1549,12 @@ namespace RT64 {
                 RenderHookDraw *drawHook = GetRenderHookDraw();
                 if (drawHook != nullptr) {
                     presentThreadStage.store("render_hook_draw", std::memory_order_release);
-                    drawHook(commandList, swapChainFramebuffer);
+                    drawHookSubmissionId = ++renderHookSubmissionId;
+                    drawHook(
+                        commandList,
+                        swapChainFramebuffer,
+                        drawHookSubmissionId
+                    );
                 }
                 if (measureGPUFrame) {
                     commandList->writeTimestamp(framePerformanceQueryPool, 3);
@@ -1627,6 +1645,7 @@ namespace RT64 {
 
             // As soon as we're done with the first render target, we notify the workload queue it can proceed.
-            if ((i == 0) && !presentIdNotified) {
+            if ((i == 0) && !presentIdNotified &&
+                (!presentWorkSubmitted || consumesInterpolationSequence)) {
                 notifyPresentId(present);
                 presentIdNotified = true;
             }
@@ -1838,6 +1857,8 @@ namespace RT64 {
                         presentationSlot->presentId = present.presentId;
                         presentationSlot->workloadId = present.workloadId;
                         presentationSlot->targetSetId = selectedTargetSetId;
+                        presentationSlot->renderHookSubmissionId =
+                            drawHookSubmissionId;
                         presentationSlot->measureGPU = measureGPUFrame;
                         presentationSlot->submitted = true;
                         presentationRetirementQueue.push_back(
@@ -1856,6 +1877,18 @@ namespace RT64 {
                     const uint64_t gpuWaitStart =
                         PerformanceProfiler::nowNanoseconds();
                     frameWorker->wait();
+                    if (drawHookSubmissionId != 0u) {
+                        RenderHookDrawComplete *drawComplete =
+                            GetRenderHookDrawComplete();
+                        if (drawComplete != nullptr) {
+                            drawComplete(drawHookSubmissionId);
+                        }
+                    }
+                    // Non-interpolated targets remain mutable; keep the manager locks through GPU sampling.
+                    if ((i == 0) && !presentIdNotified) {
+                        notifyPresentId(present);
+                        presentIdNotified = true;
+                    }
                     if (measureGPUFrame) {
                         framePerformanceQueryPool->queryResults();
                         const uint64_t *timestamps =
diff --git a/src/hle/rt64_present_queue.h b/src/hle/rt64_present_queue.h
index 5bde522..ac8ea80 100644
--- a/src/hle/rt64_present_queue.h
+++ b/src/hle/rt64_present_queue.h
@@ -47,6 +47,7 @@ namespace RT64 {
             uint64_t presentId = 0;
             uint64_t workloadId = 0;
             uint64_t targetSetId = 0;
+            uint64_t renderHookSubmissionId = 0;
             bool measureGPU = false;
             bool submitted = false;
         };
@@ -92,6 +93,7 @@ namespace RT64 {
         std::vector<std::unique_ptr<RenderCommandSemaphore>> drawSemaphores;
         std::vector<PresentationSlot> presentationSlots;
         uint32_t presentationSlotCursor = 0;
+        uint64_t renderHookSubmissionId = 0;
         std::unique_ptr<VIRenderer> viRenderer;
         std::unique_ptr<Inspector> inspector;
         std::unique_ptr<RenderQueryPool> performanceQueryPool;
diff --git a/src/render/rt64_framebuffer_renderer.cpp b/src/render/rt64_framebuffer_renderer.cpp
index b21b412..e51d5d0 100644
--- a/src/render/rt64_framebuffer_renderer.cpp
+++ b/src/render/rt64_framebuffer_renderer.cpp
@@ -1991,6 +1991,23 @@ namespace RT64 {
             dummyDepthTargetView = dummyDepthTarget->createTextureView(RenderTextureViewDesc::Texture2D(dummyDepthDesc.format));
             dummyDepthTargetTransitioned = false;
         }
+
+        // Some valid workloads contain no texture tiles.
+        if (dummyGPUTileBuffer == nullptr) {
+            dummyGPUTileBuffer = worker->device->createBuffer(
+                RenderBufferDesc::UploadBuffer(
+                    sizeof(interop::GPUTile),
+                    RenderBufferFlag::STORAGE
+                )
+            );
+            dummyGPUTileBuffer->setName(
+                "Framebuffer Renderer GPU Tile Dummy"
+            );
+            const interop::GPUTile emptyTile = {};
+            void *mappedTile = dummyGPUTileBuffer->map();
+            std::memcpy(mappedTile, &emptyTile, sizeof(emptyTile));
+            dummyGPUTileBuffer->unmap();
+        }
     }
 
 #if RT_ENABLED
@@ -2189,7 +2208,11 @@ namespace RT64 {
         descCommonSet->setBuffer(descCommonSet->instanceRenderIndices, renderIndicesBuffer.get(), RenderBufferStructuredView(sizeof(interop::RenderIndices)));
         descCommonSet->setBuffer(descCommonSet->instanceRDPParams, drawBuffers->rdpParamsBuffer.get(), RenderBufferStructuredView(sizeof(interop::RDPParams)));
         descCommonSet->setBuffer(descCommonSet->RDPTiles, drawBuffers->rdpTilesBuffer.get(), RenderBufferStructuredView(sizeof(interop::RDPTile)));
-        descCommonSet->setBuffer(descCommonSet->GPUTiles, drawBuffers->gpuTilesBuffer.get(), RenderBufferStructuredView(sizeof(interop::GPUTile)));
+        const RenderBuffer *gpuTilesBuffer = drawBuffers->gpuTilesBuffer.get();
+        if (gpuTilesBuffer == nullptr) {
+            gpuTilesBuffer = dummyGPUTileBuffer.get();
+        }
+        descCommonSet->setBuffer(descCommonSet->GPUTiles, gpuTilesBuffer, RenderBufferStructuredView(sizeof(interop::GPUTile)));
         descCommonSet->setBuffer(descCommonSet->DynamicRenderParams, drawBuffers->renderParamsBuffer.get(), RenderBufferStructuredView(sizeof(interop::RenderParams)));
 
         // Make sure the versions vector matches the texture cache size.
diff --git a/src/render/rt64_framebuffer_renderer.h b/src/render/rt64_framebuffer_renderer.h
index 7accd88..655595f 100644
--- a/src/render/rt64_framebuffer_renderer.h
+++ b/src/render/rt64_framebuffer_renderer.h
@@ -131,6 +131,7 @@ namespace RT64 {
         FramebufferRendererDescriptorTextureSet *descTextureSet = nullptr;
         std::unique_ptr<RenderTexture> dummyColorTarget;
         std::unique_ptr<RenderTexture> dummyDepthTarget;
+        std::unique_ptr<RenderBuffer> dummyGPUTileBuffer;
         std::unique_ptr<RenderTextureView> dummyColorTargetView;
         std::unique_ptr<RenderTextureView> dummyDepthTargetView;
         bool dummyColorTargetTransitioned = false;
diff --git a/src/render/rt64_render_worker.cpp b/src/render/rt64_render_worker.cpp
index 769c65e..f520a70 100644
--- a/src/render/rt64_render_worker.cpp
+++ b/src/render/rt64_render_worker.cpp
@@ -36,12 +36,36 @@ namespace RT64 {
 
         commandList = commandQueue->createCommandList();
         commandFence = device->createCommandFence();
+
+        const uint64_t workerId = std::hash<std::string>{}(name);
+        PerformanceProfiler::setMetadata(
+            "render_worker." + std::to_string(workerId),
+            name
+        );
     }
 
     RenderWorker::~RenderWorker() { }
 
     void RenderWorker::execute() {
         const uint64_t workerId = std::hash<std::string>{}(name);
+        PerformanceScope ownershipWaitScope(
+            PerformanceCategory::Wait,
+            "RenderWorker.SubmissionOwnershipWait",
+            0,
+            workerId
+        );
+        const std::scoped_lock submissionLock(submissionMutex);
+        ownershipWaitScope.end();
+        if (pendingSubmissions.load(std::memory_order_acquire) != 0u) {
+            PerformanceProfiler::recordCriticalInstant(
+                PerformanceCategory::Wait,
+                "RenderWorker.ImplicitReuseWait",
+                workerId,
+                pendingSubmissions.load(std::memory_order_relaxed),
+                pendingQueueSubmissions->load(std::memory_order_relaxed)
+            );
+            retirePendingSubmissionLocked(workerId);
+        }
         PerformanceScope queueWaitScope(
             PerformanceCategory::Wait,
             "RenderWorker.QueueMutexWait",
@@ -67,6 +91,24 @@ namespace RT64 {
         uint32_t signalSemaphoreCount) {
         const uint64_t workerId = std::hash<std::string>{}(name);
         const RenderCommandList *submittedCommandList = commandList.get();
+        PerformanceScope ownershipWaitScope(
+            PerformanceCategory::Wait,
+            "RenderWorker.SubmissionOwnershipWait",
+            0,
+            workerId
+        );
+        const std::scoped_lock submissionLock(submissionMutex);
+        ownershipWaitScope.end();
+        if (pendingSubmissions.load(std::memory_order_acquire) != 0u) {
+            PerformanceProfiler::recordCriticalInstant(
+                PerformanceCategory::Wait,
+                "RenderWorker.ImplicitReuseWait",
+                workerId,
+                pendingSubmissions.load(std::memory_order_relaxed),
+                pendingQueueSubmissions->load(std::memory_order_relaxed)
+            );
+            retirePendingSubmissionLocked(workerId);
+        }
         PerformanceScope queueWaitScope(
             PerformanceCategory::Wait,
             "RenderWorker.QueueMutexWait",
@@ -97,6 +139,10 @@ namespace RT64 {
 
     bool RenderWorker::executeIfQueueIdle() {
         const uint64_t workerId = std::hash<std::string>{}(name);
+        const std::scoped_lock submissionLock(submissionMutex);
+        if (pendingSubmissions.load(std::memory_order_acquire) != 0u) {
+            return false;
+        }
         PerformanceScope queueWaitScope(
             PerformanceCategory::Wait,
             "RenderWorker.IdleQueueMutexWait",
@@ -120,12 +166,15 @@ namespace RT64 {
         return true;
     }
 
-    void RenderWorker::wait() {
-        const uint64_t workerId = std::hash<std::string>{}(name);
+    bool RenderWorker::retirePendingSubmissionLocked(uint64_t workerId) {
         const uint32_t pendingBeforeWait = pendingSubmissions.load(
             std::memory_order_acquire
         );
-        PerformanceProfiler::recordInstant(
+        if (pendingBeforeWait == 0u) {
+            return false;
+        }
+
+        PerformanceProfiler::recordCriticalInstant(
             PerformanceCategory::Wait,
             "RenderWorker.GPUFenceWaitBegin",
             workerId,
@@ -139,39 +188,43 @@ namespace RT64 {
             workerId
         );
         commandQueue->waitForCommandFence(commandFence.get());
-        PerformanceProfiler::recordInstant(
+        PerformanceProfiler::recordCriticalInstant(
             PerformanceCategory::Wait,
             "RenderWorker.GPUFenceWaitComplete",
             workerId,
             pendingBeforeWait,
             pendingQueueSubmissions->load(std::memory_order_acquire)
         );
-        const uint32_t trackedSubmission = pendingSubmissions.load(
-            std::memory_order_acquire
-        );
-        if (trackedSubmission == 0) {
-            return;
-        }
-
-        const uint32_t previousWorkerPending = pendingSubmissions.fetch_sub(
-            1,
+        const uint32_t previousWorkerPending = pendingSubmissions.exchange(
+            0,
             std::memory_order_acq_rel
         );
         assert(previousWorkerPending > 0);
+        if (previousWorkerPending > 1u) {
+            PerformanceProfiler::recordCriticalInstant(
+                PerformanceCategory::Wait,
+                "RenderWorker.MultipleFenceOwners",
+                workerId,
+                previousWorkerPending,
+                pendingQueueSubmissions->load(std::memory_order_relaxed)
+            );
+        }
         const uint32_t previousPending = pendingQueueSubmissions->fetch_sub(
-            1,
+            previousWorkerPending,
             std::memory_order_acq_rel
         );
-        assert(previousPending > 0);
+        assert(previousPending >= previousWorkerPending);
+        return true;
+    }
+
+    void RenderWorker::wait() {
+        (void)waitIfPending();
     }
 
     bool RenderWorker::waitIfPending() {
-        // Vulkan resets waited fences; do not wait again without a new submission.
-        if (pendingSubmissions.load(std::memory_order_acquire) == 0) {
-            return false;
-        }
-        wait();
-        return true;
+        const uint64_t workerId = std::hash<std::string>{}(name);
+        const std::scoped_lock submissionLock(submissionMutex);
+        return retirePendingSubmissionLocked(workerId);
     }
 
     // RenderWorkerExecution
@@ -193,6 +239,6 @@ namespace RT64 {
     RenderWorkerExecution::~RenderWorkerExecution() {
         worker->commandList->end();
         worker->execute();
-        worker->wait();
+        worker->waitIfPending();
     }
 };
diff --git a/src/render/rt64_render_worker.h b/src/render/rt64_render_worker.h
index d4580ef..61024cb 100644
--- a/src/render/rt64_render_worker.h
+++ b/src/render/rt64_render_worker.h
@@ -18,6 +18,8 @@ namespace RT64 {
         RenderCommandQueue *commandQueue = nullptr;
         std::shared_ptr<std::mutex> commandQueueMutex;
         std::shared_ptr<std::atomic_uint32_t> pendingQueueSubmissions;
+        // Serialize Vulkan fence access, including polling.
+        std::mutex submissionMutex;
         std::atomic_uint32_t pendingSubmissions = 0;
         std::unique_ptr<RenderCommandList> commandList;
         std::unique_ptr<RenderCommandFence> commandFence;
@@ -34,6 +39,9 @@ namespace RT64 {
         bool executeIfQueueIdle();
         void wait();
         bool waitIfPending();
+
+    private:
+        bool retirePendingSubmissionLocked(uint64_t workerId);
     };
 
     // RAII convenience class for aiding opening, closing and execution of command lists inside a scope.
diff --git a/src/render/rt64_vi_renderer.cpp b/src/render/rt64_vi_renderer.cpp
index b4ff4e9..001ee33 100644
--- a/src/render/rt64_vi_renderer.cpp
+++ b/src/render/rt64_vi_renderer.cpp
@@ -10,7 +10,8 @@
 namespace RT64 {
     // VIRenderer
 
-    VIRenderer::VIRenderer() { }
+    VIRenderer::VIRenderer(std::string debugName) :
+        debugName(std::move(debugName)) { }
 
     VIRenderer::~VIRenderer() { }
 
@@ -63,6 +64,7 @@ namespace RT64 {
         if ((descriptorSet == nullptr) || (descriptorSetSampler != sampler)) {
             descriptorSet = std::make_unique<VideoInterfaceDescriptorSet>(sampler, p.device);
             descriptorSetSampler = sampler;
+            descriptorSet->get()->setName(debugName);
         }
 
         descriptorSet->setTexture(descriptorSet->gInput, p.texture, RenderTextureLayout::SHADER_READ);
@@ -122,4 +124,4 @@ namespace RT64 {
         viewport = RenderViewport(topLeftViewport.x, topLeftViewport.y, bottomRightViewport.x - topLeftViewport.x, bottomRightViewport.y - topLeftViewport.y);
         scissor = RenderRect(lround(topLeftScissor.x), lround(topLeftScissor.y), lround(bottomRightScissor.x), lround(bottomRightScissor.y));
     }
-};
\ No newline at end of file
+};
diff --git a/src/render/rt64_vi_renderer.h b/src/render/rt64_vi_renderer.h
index 8a3782a..08de966 100644
--- a/src/render/rt64_vi_renderer.h
+++ b/src/render/rt64_vi_renderer.h
@@ -12,6 +12,7 @@
 
 namespace RT64 {
     struct VIRenderer {
+        std::string debugName;
         std::unique_ptr<VideoInterfaceDescriptorSet> descriptorSet;
         const RenderSampler *descriptorSetSampler = nullptr;
 
@@ -31,9 +32,9 @@ namespace RT64 {
             bool removeBlackBorders = false;
         };
 
-        VIRenderer();
+        explicit VIRenderer(std::string debugName = "Video Interface");
         ~VIRenderer();
         void render(const RenderParams &p);
         static void getViewportAndScissor(const RenderSwapChain *swapChain, const VI &vi, hlslpp::float2 resolutionScale, uint32_t downsamplingScale, bool removeBlackBorders, RenderViewport &viewport, RenderRect &scissor);
     };
-};
\ No newline at end of file
+};
diff --git a/src/rhi/rt64_render_hooks.cpp b/src/rhi/rt64_render_hooks.cpp
index be0e037..b3a0bdf 100644
--- a/src/rhi/rt64_render_hooks.cpp
+++ b/src/rhi/rt64_render_hooks.cpp
@@ -7,6 +7,7 @@
 namespace RT64 {
     static RenderHookInit *init = nullptr;
     static RenderHookDraw *draw = nullptr;
+    static RenderHookDrawComplete *drawComplete = nullptr;
     static RenderHookPresent *present = nullptr;
     static RenderHookDeinit *deinit = nullptr;
     static RenderHookCapturePrepare *capturePrepare = nullptr;
@@ -20,6 +21,10 @@ namespace RT64 {
         return draw;
     }
 
+    RenderHookDrawComplete *GetRenderHookDrawComplete() {
+        return drawComplete;
+    }
+
     RenderHookPresent *GetRenderHookPresent() {
         return present;
     }
@@ -46,6 +51,10 @@ namespace RT64 {
         present = present_;
     }
 
+    void SetRenderHookDrawComplete(RenderHookDrawComplete *complete_) {
+        drawComplete = complete_;
+    }
+
     void SetRenderCaptureHooks(
         RenderHookCapturePrepare *prepare,
         RenderHookCaptureComplete *complete
diff --git a/src/rhi/rt64_render_hooks.h b/src/rhi/rt64_render_hooks.h
index be73617..12fdf55 100644
--- a/src/rhi/rt64_render_hooks.h
+++ b/src/rhi/rt64_render_hooks.h
@@ -8,7 +8,13 @@
 
 namespace RT64 {
     using RenderHookInit = void(RenderInterface *rhi, RenderDevice *device);
-    using RenderHookDraw = void(RenderCommandList *list, RenderFramebuffer *swapChainFramebuffer);
+    using RenderHookDraw = void(
+        RenderCommandList *list,
+        RenderFramebuffer *swapChainFramebuffer,
+        uint64_t submissionId
+    );
+    // Call only after the command list containing the draw hook retires.
+    using RenderHookDrawComplete = void(uint64_t submissionId);
     using RenderHookPresent = void();
     using RenderHookDeinit = void();
     // Optional diagnostic capture hooks.
@@ -27,6 +35,7 @@ namespace RT64 {
 
     RenderHookInit *GetRenderHookInit();
     RenderHookDraw *GetRenderHookDraw();
+    RenderHookDrawComplete *GetRenderHookDrawComplete();
     RenderHookPresent *GetRenderHookPresent();
     RenderHookDeinit *GetRenderHookDeinit();
     RenderHookCapturePrepare *GetRenderHookCapturePrepare();
@@ -34,6 +43,7 @@ namespace RT64 {
 
     void SetRenderHooks(RenderHookInit *init, RenderHookDraw *draw, RenderHookDeinit *deinit);
     void SetRenderHookPresent(RenderHookPresent *present);
+    void SetRenderHookDrawComplete(RenderHookDrawComplete *complete);
     void SetRenderCaptureHooks(
         RenderHookCapturePrepare *prepare,
         RenderHookCaptureComplete *complete
diff --git a/src/shaders/PostBlendDitherNoisePS.hlsl b/src/shaders/PostBlendDitherNoisePS.hlsl
index a007a71..247415e 100644
--- a/src/shaders/PostBlendDitherNoisePS.hlsl
+++ b/src/shaders/PostBlendDitherNoisePS.hlsl
@@ -13,11 +13,13 @@ void PSMain(
       in float4 vertexPosition : SV_POSITION
     , in float2 vertexUV : TEXCOORD
     , in float4 vertexSmoothColor : COLOR0
-    // Keep the pixel-stage register layout identical to RasterVS.
-    , in float3 vertexWorldPosition : POSITION1
-    , in float3 vertexWorldNormal : NORMAL0
-    , nointerpolation in float4 vertexFlatColor : COLOR1
+    // Keep the pixel-stage register layout identical to RasterVS.
+    , in float4 vertexWorldPosition : POSITION1
+    , in float4 vertexWorldNormal : NORMAL0
     , nointerpolation in uint renderIndex : TEXCOORD1
+    , in float4 vertexShadowPosition : TEXCOORD2
+    , in float4 vertexDynamicShadowPosition : TEXCOORD3
+    , nointerpolation in float4 vertexFlatColor : COLOR1
     , [[vk::location(0)]] [[vk::index(0)]] out float4 resultColor : SV_TARGET0
 )
 {
